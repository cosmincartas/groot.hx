;; Package metadata consumed by Forge.
(define package-name 'groot)
(define version "0.1.0")

;; Runtime dependencies used by the Helix UI adapter.
(define dependencies
  '((#:name glyph #:git-url "https://github.com/Ra77a3l3-jar/glyph.hx.git")))
